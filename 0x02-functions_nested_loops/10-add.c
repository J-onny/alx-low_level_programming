#include "main.h"

/**
 *Add integers - Check description
 * adds two integers and returns the result.
 * Return: Nothing.
 */
int add(int x, int y)
{
	int result;
	result = x + y;
	return(result);

}

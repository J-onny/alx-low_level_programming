#include "main.h"

/**
 * Description: This function returns absolute value of a number
 * Return: Absolut value of number p
 */
int _abs(int p)
{
	if (p >= 0)
		return (p);
	else
		return (p * -1);
}

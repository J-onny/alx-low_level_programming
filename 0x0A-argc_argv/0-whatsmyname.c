#include "main.h"
/**
 *@argc count of the variable passed alon with the command line

 *@argv the character variable passes to the argument
 *
 *
 *
 * */
int main(int argc , char *argv[])
{
	if (argc > 0)
	
		printf("%s\n", argv[0]);
	return 0;
}

